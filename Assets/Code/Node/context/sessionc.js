#!/usr/bin/node
"use strict" ;
export { sessionContext } ;

const sessionContext = {
  "keys": {
    "id": { "of": "/statistics/session", "for": [ "/statistics/input", "/statistics/interaction/npc", "/statistics/time" ] }
  },
} ;